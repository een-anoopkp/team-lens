/**
 * Standup Board — five-column Kanban for the active sprint.
 *
 * Stub for step 3. Full implementation lands in step 4 (board layout)
 * and step 5 (notes popover).
 */

export default function StandupBoard() {
  return (
    <div>
      <h2 style={{ marginTop: 0 }}>Standup Board</h2>
      <p className="muted">Building this in the next commit.</p>
    </div>
  );
}
