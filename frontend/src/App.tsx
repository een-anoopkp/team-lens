// Placeholder app shell. Replaced in step 1.10 with full AppShell + routes.
export default function App() {
  return (
    <main style={{ fontFamily: "system-ui, sans-serif", padding: 24 }}>
      <h1>team-lens</h1>
      <p>Backend health: <code>GET /api/v1/health</code></p>
      <p>Built out in step 1.10.</p>
    </main>
  );
}
